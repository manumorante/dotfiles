# Git Configuration

Conditional Git config that automatically switches between personal and work settings based on repository location.

- **`.gitconfig`**: Main config with conditional includes
- **`.gitconfig-personal`**: Personal repositories (`~/projects/personal/`)
- **`.gitconfig-work`**: Work repositories (`~/projects/founderz/`) with custom SSH key

